#include<bits/stdc++.h>
using namespace std;
int powerofTwo(int a,int b){
    //base case
    if(b==0){
        return 1;
    }
    //small calculation + recursive call
    return a*powerofTwo(a,b-1);
}
int main(){
  int a,b;
  cin>>a>>b;
  cout<<powerofTwo(a,b);
}
